/** Canonical route paths — single source of truth for navigation. */
export const ROUTES = {
  home: "/",
  simulator: "/simulator",
  optimizer: "/optimizer",
  liveControl: "/live-control",
  debrief: "/debrief",
};

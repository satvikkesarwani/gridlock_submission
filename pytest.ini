[pytest]
# Put the repo root on sys.path so `from backend.api.main import app` resolves
# no matter which directory pytest is invoked from.
pythonpath = .
testpaths = backend/tests
filterwarnings =
    ignore::UserWarning
